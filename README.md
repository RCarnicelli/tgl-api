# 🎸 TGL Guitar Diagram API

API que gera diagramas reais de **acordes**, **escalas** e **modos** de guitarra  
usando a biblioteca [`fretboardgtr`](https://pypi.org/project/fretboardgtr/).

---

## 🚀 Como rodar localmente

```bash
pip install -r requirements.txt
uvicorn app:app --reload
